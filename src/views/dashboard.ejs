<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Système de Gestion Portuaire</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <header>
        <h1>Système de Gestion Portuaire</h1>
        <p>Session active : Capitaine <%= user %></p>
    </header>

    <main>
        <h2>Liste des Navires Amarrés (Vue Globale)</h2>
        <table>
            <thead>
                <tr>
                    <th>Navire</th>
                    <th>Client</th>
                    <th>Arrivée</th>
                    <th>Départ</th>
                    <th>Catway</th>
                </tr>
            </thead>
            <tbody>
                <% if (boats.length > 0) { %>
                    <% boats.forEach(resa => { %>
                    <tr>
                        <td><strong><%= resa.boatName %></strong></td>
                        <td><%= resa.clientName %></td>
                        <td><%= new Date(resa.checkIn).toLocaleDateString() %></td>
                        <td><%= new Date(resa.checkOut).toLocaleDateString() %></td>
                        <td><%= resa.catwayNumber %></td>
                    </tr>
                    <% }) %>
                <% } else { %>
                    <tr><td colspan="5">Aucune réservation en cours.</td></tr>
                <% } %>
            </tbody>
        </table>
        <br>
        <a href="/catways" style="padding: 10px; background: #2c3e50; color: white; text-decoration: none;">Gérer les Catways</a>
        <br><br>
        <a href="/logout" style="color: red;">Se déconnecter</a>
    </main>
</body>
</html>